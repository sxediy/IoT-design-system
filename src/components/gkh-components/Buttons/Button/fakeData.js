import * as ivan from 'src/colors';


const buttonName = 'Простая кнопка';

const callbackFunction = () => console.log('Что-то произошло после нажатия');

const size = 'l';
const background = ivan.green;
const isBordered = true;
const isDisabled = true;

const textColor = ivan.reflexBlue;
const textSize = '24px';


export { buttonName, callbackFunction, size, background, isBordered, isDisabled, textColor, textSize };
